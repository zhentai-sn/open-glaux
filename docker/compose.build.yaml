# Maintainers only: paths are relative to the first Compose file (repository root).
services:
  agent:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: agent
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: web
